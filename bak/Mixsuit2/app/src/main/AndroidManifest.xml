<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.uk.createanet.mixsuit2" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <!--
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->
        <activity android:name=".SearchVideoActivity" >
        </activity>

        <service
            android:name=".test.MyIntentService"
            android:exported="false" />

        <activity android:name=".activity.SelectViedoActivity" >
        </activity>
        <activity android:name=".activity.SelectAudioActivity" >
        </activity>
        <activity android:name=".activity.VideoAudioActivity" >
        </activity>
        <activity android:name=".activity.GrideViewWithBaseAdapter" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.SelectAudioActivity2" >
        </activity>
    </application>

</manifest>
